# MSC Receipt WebApp - Environment Configuration
# Copy this file to .env and fill in your actual values

# ===========================
# PORT CONFIGURATION
# ===========================
# Client port (default: 8080) - Change if port 80 is already in use
CLIENT_PORT=8080

# Server port (default: 5002) - Usually no need to change
SERVER_PORT=5003

# ===========================
# SECURITY
# ===========================
# JWT Secret for token signing - REQUIRED
# Generate a secure random string (e.g., using: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-change-this

# ===========================
# EMAIL CONFIGURATION (SMTP)
# ===========================
# SMTP Server hostname (e.g., smtp.gmail.com)
SMTP_HOST=smtp.example.com

# SMTP Server port (usually 587 for TLS, 465 for SSL, 25 for unsecured)
SMTP_PORT=587

# SMTP Username (usually your email address)
SMTP_USER=your-email@example.com

# SMTP Password (or app-specific password for Gmail)
SMTP_PASS=your-email-password

# From email address (appears in sent emails)
SMTP_FROM=noreply@example.com

# ===========================
# CLIENT CONFIGURATION
# ===========================
# Client URL for CORS (e.g., http://your-server-ip:8080)
# Use the actual IP/domain where your app will be accessed
CLIENT_URL=http://localhost:8080

# ===========================
# DEPLOYMENT NOTES
# ===========================
# For Portainer deployment:
# 1. Create a .env file from this template
# 2. Fill in all required values above
# 3. Update CLIENT_URL to match your server's IP and CLIENT_PORT
# 4. Ensure all ports are available on your server
# 5. Deploy using docker-compose up -d
